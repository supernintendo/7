/// <reference path="./renderable.ts"/>

module Store {
    export class ProductList extends Renderable.Content {
        constructor(outlet: string) {
            super(outlet);
        }
    }
    export class ProductPage extends Renderable.Content {
        constructor(outlet: string) {
            super(outlet);
        }
    }
    export class ShoppingCart extends Renderable.Content {
        constructor(outlet: string) {
            super(outlet);
        }
    }
}
